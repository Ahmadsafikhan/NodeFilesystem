<!DOCTYPE html>
<html>
<head>
    <title>User List</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            flex-direction: column;
            align-items: center;
            height: 100vh;
            margin: 0;
            font-family: Arial, sans-serif;
        }

        h1 {
            margin-bottom: 20px;
        }

        table {
            border-collapse: collapse; /* Collapse borders between cells */
            width: 80%;
            max-width: 600px;
            margin: 20px;
            border: 2px solid #ccc; /* Add border to create an outline */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ccc;
            border-right: 1px solid #ccc; /* Add vertical border */
            
        }

        th {
            background-color: #f2f2f2;
            text-align: center;
        }

        a {
            display: inline-block;
            margin-top: 10px;
            padding: 8px 16px;
            text-decoration: none;
            background-color: #007BFF;
            color: #fff;
            border-radius: 5px;
            transition: background-color 0.2s ease;
        }

        a:hover {
            background-color: #0056b3;
        }

        form {
            display: inline-block;
            margin: 0;
        }

        button {
            padding: 8px 16px;
            background-color: #dc3545;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.2s ease;
        }

        button:hover {
            background-color: #c82333;
        }
        .update-button {
           margin-top: 0px;
            background-color: #23c85a;
            color: #fff;
            cursor: pointer; /* Add cursor pointer for better indication */
        }
        /* Adjust the button size to match the "Delete" button */
        .update-button,
        button {
            font-size: 14px;
        }
        .button-align{
            display: flex;
            justify-content: space-around;
            align-items: center;
        }
    </style>
</head>
<body>
    <h1>User List</h1>
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Age</th>
                <th>ID</th>
                <th>Action</th> <!-- Changed column header to "Action" -->
            </tr>
        </thead>
        <tbody>
            <% users.forEach(user => { %>
                <tr>
                    <td><%= user.name %></td>
                    <td><%= user.age %> years old</td>
                    <td><%= user.id %></td>
                    <td class="button-align">
                        <!-- Changed the "Update" button to a hyperlink -->
                        <a class="update-button" href="/users/<%= user.id %>/update">Update</a>
                        <form action="/users/<%= user.id %>?_method=DELETE" method="post">
                            <button type="submit">Delete</button>
                        </form>
                    </td>
                </tr>
            <% }); %>
        </tbody>
    </table>
    <p><a href="/users/create">Create New User</a></p>
</body>
</html>
